project_{{ bundle_basename_tolower }}_homepage:
    pattern:  /
    defaults: { _controller: {{ bundle }}:App:index }

project_{{ bundle_basename_tolower }}_render:
    pattern:  /render/{folder}/{html}
    defaults: { _controller: {{ bundle }}:App:render }
